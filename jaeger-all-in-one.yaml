apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: jaeger
  namespace: monitoring
spec:
  strategy: allInOne
  allInOne:
    resources:
      limits:
        memory: "512Mi"
        cpu: "500m"
  storage:
    type: memory
  ingress:
    enabled: true
    hosts:
      - jaeger.local
